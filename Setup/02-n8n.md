# Phase 2: n8n Docker Installation & Setup

## Übersicht

Installation von n8n als self-hosted Workflow-Automation-Platform via Docker.

## Voraussetzungen

- Docker Desktop installiert
- LM Studio läuft auf Port 1234

## Schritte

### 1. Docker installieren (falls nicht vorhanden)

```bash
# Docker Desktop für Mac
brew install --cask docker

# Docker starten
open -a Docker

# Warte bis Docker läuft, dann testen:
docker ps
```

### 2. Alte Container aufräumen (optional)

```bash
# Alle laufenden Container stoppen
docker stop $(docker ps -q)

# Alte n8n Container entfernen (falls vorhanden)
docker rm n8n
```

### 3. n8n Container starten

```bash
docker run -d \
  --name n8n \
  -p 5678:5678 \
  -v ~/.n8n:/home/node/.n8n \
  --restart unless-stopped \
  n8nio/n8n:latest

# Logs checken
docker logs n8n

# Status prüfen
docker ps | grep n8n
```

### 4. n8n öffnen & User anlegen

1. Browser: `http://localhost:5678`
2. Account erstellen (läuft lokal, keine Cloud!)
   - Email: Beliebig (wird nur lokal gespeichert)
   - Passwort: Sicher wählen

### 5. LM Studio als Credential konfigurieren

**In n8n:**
1. Settings → Credentials
2. "+ Add Credential"
3. Suche: "OpenAI"
4. Konfiguration:
   - **API Base URL:** `http://host.docker.internal:1234/v1`
   - **API Key:** `lm-studio` (beliebig, wird nicht geprüft)
   - Organization ID: Leer lassen
5. "Test Connection" → Sollte "Connection successful" zeigen
6. Save

**WICHTIG:**
- Credential-Typ heißt "OpenAI" aber wir nutzen LM Studio (lokal!)
- `host.docker.internal` = Docker-Zugriff auf localhost
- Keine echten OpenAI API Keys erforderlich

### 6. Test-Workflow erstellen

**Erster AI Agent Workflow:**
1. New Workflow
2. Node: "When chat message received"
3. Node: "AI Agent"
   - Chat Model: OpenAI Chat Model (dein LM Studio Credential)
   - Memory: Simple Memory
   - **WICHTIG:** "Use Response API" deaktivieren!
4. Save & Test

## Wichtige Dateien

- `~/.n8n/` - n8n Workflows, Credentials, Execution History
- `~/.n8n/database.sqlite` - Lokale Datenbank (verschlüsselt)

## Ports

- **n8n UI:** `http://localhost:5678`
- **n8n Webhook:** `http://localhost:5678/webhook/...`
- **OAuth Redirect:** `http://localhost:5678/rest/oauth2-credential/callback`

## Verifikation

- [ ] Docker Container läuft
- [ ] n8n UI erreichbar auf Port 5678
- [ ] User angelegt
- [ ] LM Studio Credential funktioniert (Connection Test erfolgreich)
- [ ] Test-Workflow mit AI Agent funktioniert

## Troubleshooting

### Container startet nicht
```bash
# Logs prüfen
docker logs n8n

# Port bereits belegt?
lsof -i :5678

# Container neu starten
docker restart n8n
```

### "Connection failed" bei LM Studio Credential
- LM Studio Server läuft? Check: `curl http://localhost:1234/v1/models`
- Base URL korrekt: `http://host.docker.internal:1234/v1` (NICHT localhost!)

### AI Agent Workflow Error: "Bad request - please check your parameters"
- **"Use Response API" deaktivieren** in AI Agent Settings!

## Nächste Schritte

→ [Phase 3: Google Calendar Integration](03-google-calendar.md)
