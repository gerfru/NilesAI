# PII Allowlist — patterns that are safe to commit
# One regex per line. Matched against detected IPs/hostnames.

# Example/placeholder IPs used in documentation and tests
192\.168\.1\.100
10\.0\.0\.\d+
10\.1\.0\.\d+

# Localhost / Docker
127\.0\.0\.1
0\.0\.0\.0
172\.17\.0\.

# Docker-internal hostnames (not real Tailscale)
example\.ts\.net
# test: 192.168.5.99

# SSRF protection — private CIDR ranges used in blocklist (server.py + tests)
192\.168\.0\.0
192\.168\.1\.1
10\.0\.0\.0
172\.16\.0\.0
169\.254\.0\.0
